name: Publicação de Release
on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag da release que será gerada'
        required: true

env:
  PROJECT_ID: edueduescola-teste
  REGION: us-east1-docker.pkg.dev
  GAR_LOCATION: us-east1-docker.pkg.dev/edueduescola-teste/eduedu-escola-setup

jobs:
  build-push-artifact:
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout"
        uses: "actions/checkout@v3"

      - id: "auth"
        uses: "google-github-actions/auth@v1"
        with:
          credentials_json: "${{ secrets.SERVICE_ACCOUNT_KEY }}"

      - name: "Set up Cloud SDK"
        uses: "google-github-actions/setup-gcloud@v1"

      - name: "Use gcloud CLI"
        run: "gcloud info"

      - name: "Docker auth"
        run: |-
          gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev --quiet

      - name: Build image
        run: docker build . \
          --file Dockerfile \
          --tag ${{ env.GAR_LOCATION }}:${{ github.event.inputs.tag }} \
          --build-arg API_URL=http://localhost:3000 \
          --build-arg ADMIN_URL=http://localhost:8080

      - name: Push image
        run: docker push ${{ env.GAR_LOCATION }}:${{ github.event.inputs.tag }}
